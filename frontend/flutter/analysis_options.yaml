# Project-wide static analysis configuration.
# Run via `flutter analyze` (or `dart analyze`). custom_lint plugins are
# enabled below; run them with `dart run custom_lint`.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Treat these as errors so they cannot land in CI.
    invalid_annotation_target: ignore   # codegen (freezed/json_serializable) noise
    todo: ignore
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gr.dart"
    - "lib/gen/**"
    - "build/**"

  plugins:
    - custom_lint

linter:
  rules:
    # --- Style ---
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    sort_child_properties_last: true
    avoid_redundant_argument_values: true
    use_super_parameters: true
    use_string_in_part_of_directives: true

    # --- Correctness ---
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_returning_null_for_void: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true

    # --- Imports ---
    directives_ordering: true
    avoid_relative_lib_imports: true
    always_use_package_imports: true

    # --- Documentation / API hygiene ---
    package_api_docs: false
    public_member_api_docs: false

    # Disabled — too noisy for an app (vs. a library).
    one_member_abstracts: false
    avoid_classes_with_only_static_members: false
